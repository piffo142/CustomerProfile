<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- allowBackup=false: the SQLCipher key never survives a device restore
         (Keystore keys do not transfer), so a backed-up database is unreadable
         noise. Cleaner to re-sync. -->
    <application android:allowBackup="false" android:icon="@mipmap/appicon" android:supportsRtl="true"></application>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
